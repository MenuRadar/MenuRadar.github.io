/* Token gate: verify the same repository permission required for publishing. */
(function(){
  var gate=document.getElementById('loginGate'), studio=document.getElementById('studioShell'), btn=document.getElementById('loginBtn'), input=document.getElementById('loginToken'), status=document.getElementById('loginStatus');
  async function verify(){
    var token=(input.value||'').trim();
    if(!token){status.textContent='Token Key required.';return;}
    btn.disabled=true; status.textContent='Checking Token Key…';
    try{
      var r=await fetch('https://api.github.com/repos/MenuRadar/MenuRadar.github.io',{method:'GET',headers:{Authorization:'Bearer '+token,Accept:'application/vnd.github+json'}});
      if(r.status===401) throw new Error('Token Key invalid or expired.');
      if(r.status===403) throw new Error('Token valid hai lekin repository access/permission nahi hai.');
      if(r.status===404) throw new Error('Token ko MenuRadar repository ka access nahi mila.');
      if(!r.ok) throw new Error('GitHub connection failed ('+r.status+').');
      var repo=await r.json();
      if(!repo || repo.full_name!=='MenuRadar/MenuRadar.github.io') throw new Error('Wrong repository access.');
      sessionStorage.setItem('menuradar_admin_token',token);
      var gh=document.getElementById('ghToken'); if(gh) gh.value=token;
      gate.hidden=true; studio.hidden=false; if(typeof loadArticles==='function') loadArticles();
    }catch(e){
      sessionStorage.removeItem('menuradar_admin_token');
      status.textContent=e.message||'Connection failed. Token Key check nahi ho saki.';
    }finally{btn.disabled=false;}
  }
  btn.onclick=function(e){e.preventDefault();verify()};
  input.onkeydown=function(e){if(e.key==='Enter'){e.preventDefault();verify()}};
  var saved=sessionStorage.getItem('menuradar_admin_token');
  if(saved){input.value=saved;verify();}
})();
var $=function(id){return document.getElementById(id)};
var images=[],coverImage=null,sectionCount=0,editingPath='';
function esc(s){return String(s==null?'':s).replace(/[&<>\"]/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]})}
function slugify(s){return String(s||'').toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}
async function ghTokenValue(){return sessionStorage.getItem('menuradar_admin_token')||$('ghToken').value.trim()}
async function loadArticles(){var list=$('articleList');if(!list)return;list.innerHTML='<p class="hint">Loading all published articles…</p>';try{var token=await ghTokenValue(),repo=$('repo').value.trim(),branch=$('branch').value.trim()||'main',headers={Authorization:'Bearer '+token,Accept:'application/vnd.github+json'},seen={},files=[];function addPath(p){if(!p||seen[p]||p.indexOf('admin/')===0||p==='index.html'||p==='404.html')return;if(/\/index\.html$/.test(p)){seen[p]=1;files.push({path:p,type:'blob'})}}var treeR=await fetch('https://api.github.com/repos/'+repo+'/git/trees/'+encodeURIComponent(branch)+'?recursive=1',{headers:headers});if(treeR.ok){var tree=await treeR.json();(tree.tree||[]).forEach(function(x){if(x.type==='blob')addPath(x.path)})}var smR=await fetch('https://api.github.com/repos/'+repo+'/contents/sitemap.xml?ref='+encodeURIComponent(branch),{headers:headers});if(smR.ok){var sm=await smR.json(),xml=decodeGitHub(sm.content),doc=new DOMParser().parseFromString(xml,'application/xml');[].slice.call(doc.querySelectorAll('url loc')).forEach(function(n){var u=(n.textContent||'').trim();if(/^https:\/\/menuradar\.github\.io\//.test(u)){var p=u.replace('https://menuradar.github.io/','').replace(/\/$/,'');if(p&&p!=='index.html'&&p!=='404.html')addPath(p+'/index.html')}})}if(!files.length)throw new Error('No published article pages were found.');window.menuArticles=files;renderArticleList()}catch(e){list.innerHTML='<p class="hint">'+esc(e.message)+'</p>'}}function renderArticleList(){var list=$('articleList'),q=($('articleSearch').value||'').toLowerCase().trim(),country=$('articleCountry').value;var files=(window.menuArticles||[]).filter(function(x){return(!country||x.path.toLowerCase().indexOf(country.toLowerCase()+'/')===0)&&(!q||x.path.toLowerCase().indexOf(q)>=0)});if(!files.length){list.innerHTML='<p class="hint">No articles found.</p>';return}var names=[['chick-fil-a','Chick-fil-A'],['burger-king','Burger King'],['mcdonalds','McDonald’s'],['pizza-hut','Pizza Hut'],['papa-johns','Papa John’s'],['taco-bell','Taco Bell'],['starbucks','Starbucks'],['dunkin','Dunkin'],['dominos','Domino’s'],['subway','Subway'],['popeyes','Popeyes'],['wendys','Wendy’s'],['kfc','KFC']];var countries=[['usa','USA'],['uk','UK'],['france','France'],['brazil','Brazil'],['australia','Australia']];function countryFor(path){var p=path.split('/').filter(Boolean),key=(p[0]||'').toLowerCase();for(var i=0;i<countries.length;i++){if(key===countries[i][0])return countries[i][1]}return 'Other / General'}function categoryFor(path){var low=path.toLowerCase();for(var i=0;i<names.length;i++){if(low.indexOf('/'+names[i][0]+'-')>=0||low.indexOf('/'+names[i][0]+'/')>=0||low.indexOf('/'+names[i][0]+'menu')>=0)return names[i][1]}return 'Other Articles'}var groups={};files.forEach(function(x){var co=countryFor(x.path),cat=categoryFor(x.path);if(!groups[co])groups[co]={};if(!groups[co][cat])groups[co][cat]=[];groups[co][cat].push(x)});var countryOrder=['USA','UK','France','Brazil','Australia','Other / General'];function prettyPath(x){var p=x.path.split('/').filter(Boolean);return(p[p.length-1]||'article').replace(/-/g,' ')}function countryRank(a){var i=countryOrder.indexOf(a);return i<0?999:i}list.innerHTML=Object.keys(groups).sort(function(a,b){return countryRank(a)-countryRank(b)||a.localeCompare(b)}).map(function(co){var cats=groups[co],total=Object.keys(cats).reduce(function(n,k){return n+cats[k].length},0);return '<div class="country-category"><button class="country-head country-toggle"><span><b>🌎 '+esc(co)+'</b><small>'+total+' articles</small></span><span class="category-chevron">›</span></button><div class="country-content" hidden>'+Object.keys(cats).sort(function(a,b){return a==='Other Articles'?1:b==='Other Articles'?-1:a.localeCompare(b)}).map(function(cat){var fs=cats[cat].sort(function(a,b){return a.path.localeCompare(b.path)});return '<div class="sub-category"><button class="sub-category-head sub-category-toggle"><span><b>📁 '+esc(cat)+'</b><small>'+fs.length+' articles</small></span><span class="category-chevron">›</span></button><div class="sub-category-articles" hidden>'+fs.map(function(x){return '<div class="article-row"><div><strong>'+esc(prettyPath(x))+'</strong><small>'+esc(x.path)+'</small></div><button class="small edit-article" data-path="'+esc(x.path)+'">Edit</button></div>'}).join('')+'</div></div>'}).join('')+'</div></div>'}).join('');[].slice.call(document.querySelectorAll('.country-toggle')).forEach(function(b){b.onclick=function(){var box=b.nextElementSibling;box.hidden=!box.hidden;b.querySelector('.category-chevron').textContent=box.hidden?'›':'⌄'}});[].slice.call(document.querySelectorAll('.sub-category-toggle')).forEach(function(b){b.onclick=function(){var box=b.nextElementSibling;box.hidden=!box.hidden;b.querySelector('.category-chevron').textContent=box.hidden?'›':'⌄'}});[].slice.call(document.querySelectorAll('.edit-article')).forEach(function(b){b.onclick=function(){loadExistingArticle(b.dataset.path)}})}
async function loadExistingArticle(path){try{var token=await ghTokenValue(),repo=$('repo').value.trim(),r=await ghGet(token,repo,path);var raw=decodeGitHub(r.content),doc=new DOMParser().parseFromString(raw,'text/html'),d={};editingPath=path;d.title=doc.title||'';d.desc=(doc.querySelector('meta[name="description"]')||{}).content||'';d.keyword=(doc.querySelector('h1')||{}).textContent||'';d.loc='';var hero=doc.querySelector('.menu-hero .lead');d.intro=hero?hero.textContent.trim():'';var notice=doc.querySelector('.menu-notice b');d.address=notice?notice.textContent.trim():'';var eyebrow=doc.querySelector('.menu-hero .eyebrow');d.brand=eyebrow?eyebrow.textContent.split(' MENU')[0].trim():'';var parts=path.split('/');d.country=parts.length>2?parts[0]:'';d.slug=parts.length>2?parts[1]:parts[0];$('brand').value=d.brand;$('location').value='';$('address').value=d.address;$('keyword').value=d.keyword;$('slug').value=d.slug;$('country').value=d.country;$('intro').value=d.intro;$('metaTitle').value=d.title;$('metaDescription').value=d.desc;$('sections').innerHTML='';sectionCount=0;doc.querySelectorAll('.menu-section').forEach(function(sec){var h=sec.querySelector('h2'),p=sec.querySelector('.menu-section-title p'),icon=sec.querySelector('.category-icon');addSection({name:h?h.textContent.trim():'Menu',icon:icon?icon.textContent.trim():'🍽️',desc:p?p.textContent.trim():'',items:[].slice.call(sec.querySelectorAll('.menu-card')).map(function(card){var n=card.querySelector('h3'),pr=card.querySelector('.menu-price'),note=card.querySelector('p');return{name:n?n.textContent.trim():'',price:pr?pr.textContent.trim():'',note:note?note.textContent.trim():''}})});});$('links').value=[].slice.call(doc.querySelectorAll('.related a')).map(function(a){return a.getAttribute('href')+'|'+a.textContent.trim()}).join('\n');coverImage=null;var cover=doc.querySelector('.menu-cover img');if(cover){coverImage={id:crypto.randomUUID(),file:null,data:'',url:cover.src,alt:cover.alt||'',caption:''}};renderCover();images=[];doc.querySelectorAll('.article-image img').forEach(function(im){images.push({id:crypto.randomUUID(),file:null,data:'',url:im.src,alt:im.alt||'',caption:im.closest('figure')&&im.closest('figure').querySelector('figcaption')?im.closest('figure').querySelector('figcaption').textContent.trim():''})});renderImages();$('publishBtn').textContent='Update Article';refreshPreview();window.scrollTo({top:document.querySelector('.intro').offsetTop,behavior:'smooth'});setStatus('Loaded for editing: '+path,true);}catch(e){alert(e.message||e)}}
function setStatus(s,good){$('status').textContent=s;$('status').style.color=good?'#16834b':''}
function addSection(d){d=d||{};sectionCount++;var el=document.createElement('div');el.className='section-editor';el.innerHTML='<div class="section-top"><label>Section name<input class="s-name" value="'+esc(d.name||'Chicken')+'"></label><label>Icon<input class="s-icon" value="'+esc(d.icon||'🍗')+'"></label><label>Description<input class="s-desc" value="'+esc(d.desc||'Menu options and reference pricing for this section.')+'"></label><button class="remove-section">Remove</button></div><div class="items"></div><button class="add-item">+ Add item</button>';$('sections').appendChild(el);(d.items||[{name:'',price:''}]).forEach(function(x){addItem(el,x)});el.querySelector('.add-item').onclick=function(){addItem(el)};el.querySelector('.remove-section').onclick=function(){el.remove()}}
function addItem(section,d){d=d||{};var row=document.createElement('div');row.className='item-row';row.innerHTML='<input class="i-name" placeholder="Item name" value="'+esc(d.name||'')+'"><input class="i-price" placeholder="Price / reference" value="'+esc(d.price||'')+'"><input class="i-note" placeholder="Short note (optional)" value="'+esc(d.note||'')+'"><button>×</button>';section.querySelector('.items').appendChild(row);row.querySelector('button').onclick=function(){row.remove()}}
function articleData(){var brand=$('brand').value.trim()||'Restaurant',loc=$('location').value.trim(),address=$('address').value.trim(),keyword=$('keyword').value.trim()||brand+' Menu',slug=$('slug').value.trim()||slugify(brand+'-'+address),country=$('country').value.trim()||'usa';var sections=[].slice.call(document.querySelectorAll('.section-editor')).map(function(s){return{name:s.querySelector('.s-name').value.trim(),icon:s.querySelector('.s-icon').value.trim(),desc:s.querySelector('.s-desc').value.trim(),items:[].slice.call(s.querySelectorAll('.item-row')).map(function(r){return{name:r.querySelector('.i-name').value.trim(),price:r.querySelector('.i-price').value.trim(),note:r.querySelector('.i-note').value.trim()}}).filter(function(x){return x.name})}}).filter(function(x){return x.name});var title=$('metaTitle').value.trim()||keyword+' & Prices '+(loc?'— '+loc:'')+' | MenuRadar';var desc=$('metaDescription').value.trim()||keyword+' '+(loc?'for '+loc:'')+' with menu categories, popular items and reference pricing. Prices and availability may vary by restaurant and ordering channel.';var links=$('links').value.split('\n').map(function(x){return x.trim()}).filter(Boolean).map(function(x){var a=x.split('|');return{href:a.shift().trim(),text:(a.join('|')||x).trim()}});return{brand:brand,loc:loc,address:address,keyword:keyword,slug:slug,country:country,intro:$('intro').value.trim(),sections:sections,title:title,desc:desc,links:links}}


function aiSetStatus(s,good){var el=$('aiStatus');if(!el)return;el.textContent=s;el.style.color=good?'#16834b':''}
function extractAIText(r){
  if(r.output_text)return r.output_text;
  var out=r.output||[],parts=[];
  out.forEach(function(x){(x.content||[]).forEach(function(c){if(c.type==='output_text'&&c.text)parts.push(c.text)})});
  return parts.join('');
}
function aiPrompt(source){
  return 'You are MenuRadar Article AI. Transform the supplied source into structured restaurant-menu article data. Preserve factual menu item names, prices and location information from the source; never invent exact prices. If a price is absent, use an empty string. Rewrite prose into original, useful wording rather than copying long passages. Create natural SEO coverage without keyword stuffing. Return ONLY valid JSON matching this shape: {"brand":"","location":"","address":"","country":"usa|uk|france|brazil|australia","slug":"","keyword":"","title":"","metaDescription":"","intro":"","sections":[{"name":"","icon":"","description":"","items":[{"name":"","price":"","note":""}]}],"internalLinks":[{"href":"","text":""}],"imageAlt":"","imageCaption":""}. Use an empty array when data is unavailable. Keep section icons as emoji. Country should be inferred only when supported by the source; otherwise use usa. Source:\n\n'+source;
}
async function runAI(){
  var key=($('openaiKey').value||'').trim(),source=($('aiSource').value||'').trim(),model=($('aiModel').value||'gpt-5.6-luna').trim();
  if(!key){aiSetStatus('OpenAI API key add karo.');return}
  if(!source){aiSetStatus('Pehle source content paste karo.');return}
  if(source.length>120000){aiSetStatus('Source bohat lamba hai. Pehle isay thora shorten karo.');return}
  $('runAI').disabled=true;aiSetStatus('AI source analyze kar raha hai…');
  try{
    var r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+key},body:JSON.stringify({
      model:model,input:aiPrompt(source),store:false,
      text:{format:{type:'json_object'}}
    })});
    var raw=await r.text(),j={};try{j=JSON.parse(raw)}catch(e){}
    if(!r.ok)throw new Error((j.error&&j.error.message)||'OpenAI request failed ('+r.status+').');
    var text=extractAIText(j).trim(),data;try{data=JSON.parse(text)}catch(e){throw new Error('AI ne valid JSON return nahi kiya.')}
    if(!data||!data.keyword)throw new Error('AI output incomplete hai.');
    $('brand').value=data.brand||$('brand').value;
    $('location').value=data.location||'';
    $('address').value=data.address||'';
    $('country').value=data.country||'usa';
    $('slug').value=data.slug||slugify((data.brand||'restaurant')+'-'+(data.location||'menu'));
    $('keyword').value=data.keyword;
    $('metaTitle').value=data.title||data.keyword+' & Prices | MenuRadar';
    $('metaDescription').value=data.metaDescription||data.keyword+' menu, popular items and reference pricing. Prices and availability may vary by location and ordering channel.';
    $('intro').value=data.intro||'Explore the menu by category with reference pricing and popular choices.';
    $('links').value=(data.internalLinks||[]).map(function(x){return (x.href||'')+'|'+(x.text||x.href||'')}).filter(Boolean).join('\n');
    $('sections').innerHTML='';sectionCount=0;
    (data.sections||[]).forEach(function(s){addSection({name:s.name||'Menu',icon:s.icon||'🍽️',desc:s.description||'Menu options and reference pricing.',items:(s.items||[]).map(function(i){return{name:i.name||'',price:i.price||'',note:i.note||''}})})});
    if(!document.querySelector('.section-editor'))addSection({name:'Menu',icon:'🍽️'});
    refreshPreview();
    aiSetStatus('AI analysis complete — article fields, SEO and menu sections filled. Preview check karo.',true);
    document.querySelector('.preview-panel').scrollIntoView({behavior:'smooth',block:'start'});
  }catch(e){aiSetStatus(e.message||String(e));alert(e.message||String(e))}
  finally{$('runAI').disabled=false}
}
$('useRawForAI').onclick=function(){
  var raw=($('rawContent').value||'').trim();
  if(!raw){aiSetStatus('Full Content box mein pehle source paste karo.');return}
  $('aiSource').value=raw;
  aiSetStatus('Full Content AI Writer mein copy ho gaya — ab Analyze with AI dabao.',true);
  $('aiSource').focus();
};
$('runAI').onclick=runAI;
$('clearAI').onclick=function(){$('aiSource').value='';aiSetStatus('Ready for AI analysis.')};

function cleanImportText(s){return String(s||'').replace(/\\r/g,'').trim()}
function parsePriceText(s){var m=String(s||'').match(/(?:\\$|£|€|R\\$)\\s?\\d+(?:[.,]\\d{1,2})?(?:\\s?[-–]\\s?(?:\\$|£|€|R\\$)?\\s?\\d+(?:[.,]\\d{1,2})?)?/);return m?m[0]:''}
function parseImportedContent(raw){
  raw=cleanImportText(raw);if(!raw)throw new Error('Pehle content paste karo.');
  var lines=raw.split(/\\n/).map(function(x){return x.trim()}).filter(Boolean), title='',intro=[],sections=[],current=null;
  function looksHeading(s){return /^(#{1,6}\\s+|(?:menu|popular|breakfast|lunch|dinner|chicken|sandwich|burgers?|sides?|drinks?|desserts?|beverages?|meals?|combos?|snacks?|salads?|pizza|pasta|coffee|tea|kids?|value|specials?|appetizers?|entrees?|main courses?|prices?|menu items?)(?:\\s|$))/i.test(s)}
  function addSection(name){current={name:name.replace(/^#{1,6}\\s+/,'').replace(/:$/,''),icon:'🍽️',desc:'Menu options and reference pricing.',items:[]};sections.push(current)}
  function addItem(line){
    var clean=line.replace(/^[-*•]+\\s*/,'').replace(/^\\d+[.)]\\s*/,'').trim(), price=parsePriceText(clean), name=price?clean.replace(price,'').replace(/\\s*[-–—:|]\\s*$/,'').trim():clean;
    if(name.length>1){if(!current)addSection('Menu');current.items.push({name:name,price:price,note:''})}
  }
  title=lines[0].replace(/^#{1,6}\\s+/,'').trim();
  for(var i=1;i<lines.length;i++){
    var line=lines[i];
    if(/^#{1,6}\\s+/.test(line)){addSection(line);continue}
    if(/^(?:={3,}|-{3,})$/.test(line))continue;
    if(looksHeading(line)&&line.length<70&&!/[.!?]{2,}/.test(line)){addSection(line);continue}
    if(/^[-*•]+\\s+/.test(line)||/\\s(?:[-–—|:])\\s*(?:\\$|£|€|R\\$)?\\d/.test(line)||parsePriceText(line)){
      addItem(line);continue
    }
    if(current&&current.items.length===0)current.desc=(current.desc==='Menu options and reference pricing.'?line:current.desc+' '+line);
    else if(!current)intro.push(line);
    else if(current.items.length===0)current.desc+=' '+line;
  }
  if(!sections.length)addSection('Menu');
  sections=sections.filter(function(s){return s.items.length||s.name});
  return {title:title||'Restaurant Menu',intro:intro.join(' '),sections:sections};
}
function importFullContent(){
  try{
    var raw=$('rawContent').value.trim(),p=parseImportedContent(raw),title=p.title;
    $('brand').value=title.replace(/\\s+(menu|menus).*$/i,'').trim()||$('brand').value;
    $('keyword').value=title;
    $('intro').value=p.intro||'This guide organizes the menu by category with reference pricing and popular choices.';
    $('metaTitle').value=title+' & Prices | MenuRadar';
    $('metaDescription').value=title+' with menu categories, popular items and reference pricing. Prices and availability may vary by location and ordering channel.';
    $('sections').innerHTML='';sectionCount=0;
    p.sections.forEach(function(s){addSection(s)});
    refreshPreview();
    $('importStatus').textContent='Content read, structured and designed. Ab preview check karke publish karo.';
    $('importStatus').style.color='#16834b';
    window.scrollTo({top:document.querySelector('.panel:nth-of-type(4)').offsetTop,behavior:'smooth'});
  }catch(e){$('importStatus').textContent=e.message||String(e);$('importStatus').style.color=''}
}
$('parseContent').onclick=importFullContent;$('clearContent').onclick=function(){$('rawContent').value='';$('importStatus').textContent='Paste content and let Article Studio structure it.'};$('addSection').onclick=function(){addSection()};$('refreshArticles').onclick=loadArticles;$('articleSearch').oninput=renderArticleList;$('articleCountry').onchange=renderArticleList;
$('coverImage').onchange=function(e){var file=e.target.files&&e.target.files[0];if(!file)return;var id=crypto.randomUUID(),reader=new FileReader();reader.onload=function(){coverImage={id:id,file:file,data:reader.result,url:'',alt:$('keyword').value.trim()||'restaurant menu cover',caption:''};renderCover()};reader.readAsDataURL(file)};document.querySelector('.cover-box').onclick=function(){ $('coverImage').click() };document.querySelector('.upload-box:not(.cover-box)').onclick=function(){ $('images').click() };$('images').onchange=function(e){[].slice.call(e.target.files).forEach(function(file){var id=crypto.randomUUID(),reader=new FileReader();reader.onload=function(){images.push({id:id,file:file,data:reader.result,url:'',alt:'',caption:''});renderImages()};reader.readAsDataURL(file)})};
function renderCover(){var box=$('coverPreview');if(!box)return;if(!coverImage){box.innerHTML='<div class="cover-empty">No cover image selected.</div>';return}box.innerHTML='<div class="cover-card"><img src="'+(coverImage.url||coverImage.data)+'" alt=""><div class="body"><label>Cover alt text<input id="coverAlt" value="'+esc(coverImage.alt||$('keyword').value||'restaurant menu cover')+'"></label><button class="remove" id="removeCover">Remove cover</button></div></div>';$('coverAlt').oninput=function(){if(coverImage)coverImage.alt=this.value};$('removeCover').onclick=function(){coverImage=null;$('coverImage').value='';renderCover()}}function renderImages(){$('imageList').innerHTML=images.map(function(im){return '<div class="image-card"><img src="'+(im.url||im.data)+'" alt=""><div class="body"><label>Alt text<input data-img="'+im.id+'" data-field="alt" value="'+esc(im.alt||$('keyword').value||'restaurant menu')+'"></label><label>Caption<input data-img="'+im.id+'" data-field="caption" value="'+esc(im.caption||'MenuRadar restaurant menu image')+'"></label><button class="remove" data-remove="'+im.id+'">Remove</button></div></div>'}).join('');[].slice.call(document.querySelectorAll('[data-field]')).forEach(function(x){x.oninput=function(){var im=images.find(function(a){return a.id===x.dataset.img});if(im)im[x.dataset.field]=x.value}});[].slice.call(document.querySelectorAll('[data-remove]')).forEach(function(x){x.onclick=function(){images=images.filter(function(a){return a.id!==x.dataset.remove});renderImages()}})}
async function uploadOneImage(key,im){if(im.url)return;var fd=new FormData();fd.append('key',key);fd.append('image',im.file);var r=await fetch('https://api.imgbb.com/1/upload',{method:'POST',body:fd}),j=await r.json();if(!j.success)throw new Error('Image upload failed: '+((j.error&&j.error.message)||'ImgBB error'));im.url=j.data.display_url;im.deleteUrl=j.data.delete_url}async function uploadImages(key){if(coverImage&&!coverImage.url){await uploadOneImage(key,coverImage);setStatus('Cover image uploaded.',true)}for(var i=0;i<images.length;i++){var im=images[i];if(im.url)continue;var fd=new FormData();fd.append('key',key);fd.append('image',im.file);var r=await fetch('https://api.imgbb.com/1/upload',{method:'POST',body:fd}),j=await r.json();if(!j.success)throw new Error('Image upload failed: '+((j.error&&j.error.message)||'ImgBB error'));im.url=j.data.display_url;im.deleteUrl=j.data.delete_url;setStatus('Uploaded image '+(i+1)+' of '+images.length+'…',true)}}
function renderArticle(d){var coverHtml=coverImage&&coverImage.url?'<figure class="menu-cover"><img src="'+esc(coverImage.url)+'" alt="'+esc(coverImage.alt||d.keyword)+'" loading="eager" width="1200" height="800"></figure>':'';var imageHtml=images.filter(function(x){return x.url}).map(function(x){return '<figure class="article-image"><img src="'+esc(x.url)+'" alt="'+esc(x.alt||d.keyword)+'" loading="lazy" width="1200" height="800"><figcaption>'+esc(x.caption||'')+'</figcaption></figure>'}).join('');var sections=d.sections.map(function(s){var items=s.items.map(function(i){return '<article class="menu-card"><div class="menu-card-top"><h3>'+esc(i.name)+'</h3><span class="menu-price">'+esc(i.price||'Reference')+'</span></div>'+(i.note?'<p>'+esc(i.note)+'</p>':'')+'</article>'}).join('');return '<section class="menu-section"><div class="menu-section-head"><div class="menu-section-title"><span class="category-icon">'+esc(s.icon)+'</span><div><h2>'+esc(s.name)+'</h2><p>'+esc(s.desc)+'</p></div></div><p>'+s.items.length+' listed</p></div><div class="menu-grid">'+items+'</div></section>'}).join('');var links=d.links.length?d.links.map(function(l){return '<a href="'+esc(l.href)+'">'+esc(l.text)+'</a>'}).join(''):'<a href="/">MenuRadar Home</a>';var imgCss=((coverImage&&coverImage.url)||images.some(function(x){return x.url}))?'.menu-cover{margin:0 0 24px;border-radius:20px;overflow:hidden;background:#fff;border:1px solid #e5e7eb}.menu-cover img{display:block;width:100%;height:auto;max-height:620px;object-fit:cover}.article-image{margin:22px 0;border-radius:18px;overflow:hidden;background:#fff;border:1px solid #e5e7eb}.article-image img{display:block;width:100%;height:auto;max-height:620px;object-fit:cover}.article-image figcaption{padding:10px 14px;color:#667085;font-size:13px}':'';return '<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>'+esc(d.title)+'</title><meta name="description" content="'+esc(d.desc)+'"><meta name="robots" content="index,follow"><link rel="canonical" href="https://menuradar.github.io/'+esc(d.country)+'/'+esc(d.slug)+'/"><link rel="stylesheet" href="/styles.css"><style>'+imgCss+'</style></head><body><header class="site-header"><a class="logo" href="/">Menu<span>Radar</span></a></header><section class="menu-hero"><div class="menu-hero-inner">'+coverHtml+'<p class="eyebrow">'+esc(d.brand)+' MENU • MENURADAR</p><h1>'+esc(d.keyword)+(d.loc?' — '+esc(d.loc):'')+'</h1><p class="lead">'+esc(d.intro||d.desc)+'</p></div></section><main class="menu-layout"><div class="menu-main">'+imageHtml+'<div class="menu-notice"><span>📍</span><div><b>'+esc(d.address||d.loc||d.brand)+'</b><span>Menu information is provided as reference content. Availability and prices can vary by restaurant, market and ordering channel.</span></div></div>'+sections+'<section class="section seo-coverage"><h2>'+esc(d.keyword)+' &amp; Prices</h2><p>Looking for the '+esc(d.keyword)+(d.loc?' in '+esc(d.loc):'')+'? This guide organizes the menu by category so visitors can quickly compare choices, popular items and reference pricing.</p><p>The '+esc(d.keyword)+' can change by location and ordering channel. Check the restaurant or delivery checkout for final availability and pricing.</p><div class="related">'+links+'</div></section></div></main><footer><div class="footer-inner"><b>MenuRadar</b><span>Restaurant Menus, Prices &amp; More</span><div><a href="/privacy.html">Privacy</a><a href="/about.html">About</a></div></div></footer></body></html>'}
function refreshPreview(){$('preview').srcdoc=renderArticle(articleData())}$('previewBtn').onclick=refreshPreview;
async function ghGet(token,repo,path){var r=await fetch('https://api.github.com/repos/'+repo+'/contents/'+path,{headers:{Authorization:'Bearer '+token,Accept:'application/vnd.github+json'}});if(!r.ok)throw new Error('GitHub read failed: '+r.status);return r.json()}
async function ghPut(token,repo,path,content,message,sha,branch){var body={message:message,content:btoa(unescape(encodeURIComponent(content))),branch:branch};if(sha)body.sha=sha;var r=await fetch('https://api.github.com/repos/'+repo+'/contents/'+path,{method:'PUT',headers:{Authorization:'Bearer '+token,Accept:'application/vnd.github+json','Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw new Error('GitHub publish failed: '+r.status+' '+(await r.text()).slice(0,300));return r.json()}
function decodeGitHub(s){return decodeURIComponent(escape(atob(s.replace(/\n/g,''))))}
function insertHomepageLink(html,d){var href='/'+d.country+'/'+d.slug+'/';if(html.indexOf('href="'+href+'"')>=0)return html;var card='<a href="'+href+'"><strong>🍽️ '+esc(d.brand)+' — '+esc(d.address||d.loc||d.slug)+'</strong><br><small>MenuRadar restaurant/menu guide</small></a>';var marker='<div class="topic-grid">',i=html.indexOf(marker);return i<0?html:html.slice(0,i+marker.length)+'\n'+card+html.slice(i+marker.length)}
function insertSitemap(xml,d){var loc='https://menuradar.github.io/'+d.country+'/'+d.slug+'/';if(xml.indexOf('<loc>'+loc+'</loc>')>=0)return xml;var entry='  <url><loc>'+loc+'</loc><changefreq>weekly</changefreq><priority>0.7</priority></url>\n';return xml.replace('</urlset>',entry+'</urlset>')}
async function publish(){try{var key=$('imgKey').value.trim(),token=sessionStorage.getItem('menuradar_admin_token')||$('ghToken').value.trim(),repo=$('repo').value.trim(),branch=$('branch').value.trim()||'main',d=articleData();if(!token)throw new Error('Add the GitHub token first.');if((images.length||coverImage)&&!key)throw new Error('Add the image API key first.');if(!d.slug)throw new Error('Add a URL slug.');$('publishBtn').disabled=true;setStatus('Uploading images…');if(images.length||coverImage)await uploadImages(key);var html=renderArticle(d),path=editingPath||d.country+'/'+d.slug+'/index.html',existing=null;try{existing=await ghGet(token,repo,path)}catch(e){}await ghPut(token,repo,path,html,'Publish '+d.keyword+' — '+(d.loc||d.slug),existing&&existing.sha,branch);setStatus(editingPath?'Article updated. Updating homepage and sitemap…':'Article published. Updating homepage and sitemap…',true);var home=await ghGet(token,repo,'index.html'),oldHome=decodeGitHub(home.content),newHome=insertHomepageLink(oldHome,d);if(newHome!==oldHome)await ghPut(token,repo,'index.html',newHome,'Add article to MenuRadar homepage',home.sha,branch);var sm=await ghGet(token,repo,'sitemap.xml'),oldSm=decodeGitHub(sm.content),newSm=insertSitemap(oldSm,d);if(newSm!==oldSm)await ghPut(token,repo,'sitemap.xml',newSm,'Add article to sitemap',sm.sha,branch);setStatus((editingPath?'Updated':'Published')+' + homepage + sitemap updated: '+path,true);editingPath='';$('publishBtn').textContent='Upload images & Publish';refreshPreview();loadArticles()}catch(e){setStatus(e.message||String(e));alert(e.message||e)}finally{$('publishBtn').disabled=false}}
$('publishBtn').onclick=publish;$('saveImgKey').onclick=function(){localStorage.setItem('menuradar_img_key',$('imgKey').value.trim());setStatus('Image key saved on this device.',true)};var saved=localStorage.getItem('menuradar_img_key');if(saved)$('imgKey').value=saved;
addSection({name:'Chicken',icon:'🍗'});addSection({name:'Sides',icon:'🍟'});addSection({name:'Drinks',icon:'🥤'});refreshPreview();
async function runAutopilotAgent(){
  var token=sessionStorage.getItem('menuradar_admin_token')||($('ghToken')&&$('ghToken').value.trim()),source=($('agentSource').value||'').trim(),url=($('agentSourceUrl').value||'').trim(),images=($('agentMaxImages').value||'4');
  if(!token){$('agentStatus').textContent='Pehle Token Key se login karo.';return}
  if(!source&&!url){$('agentStatus').textContent='Source content ya Source URL do.';return}\n  if(source.length>60000){$('agentStatus').textContent='Source content 60,000 characters se zyada hai. Source URL use karo ya content ko shorten karo.';return}
  $('runAgent').disabled=true;$('agentStatus').textContent='Autopilot workflow start ho raha hai…';
  try{
    var r=await fetch('https://api.github.com/repos/MenuRadar/MenuRadar.github.io/actions/workflows/menuradar-agent.yml/dispatches',{method:'POST',headers:{Authorization:'Bearer '+token,Accept:'application/vnd.github+json','Content-Type':'application/json'},body:JSON.stringify({ref:'main',inputs:{source:source,source_url:url,max_images:images,model:($('aiModel').value||'gpt-5.6-luna')}})});
    if(!r.ok){var t=await r.text();throw new Error('Agent dispatch failed ('+r.status+'): '+t.slice(0,180))}
    $('agentStatus').textContent='Agent start ho gaya. GitHub Actions source analyze, images process aur article publish karega.';$('agentStatus').style.color='#16834b';
  }catch(e){$('agentStatus').textContent=e.message||String(e);$('agentStatus').style.color=''}finally{$('runAgent').disabled=false}
}
$('runAgent').onclick=runAutopilotAgent;
$('agentFromAI').onclick=function(){var s=($('aiSource').value||'').trim();if(!s){$('agentStatus').textContent='AI Source box empty hai.';return}$('agentSource').value=s;$('agentStatus').textContent='AI Source Autopilot mein copy ho gaya.';$('agentSource').focus()};
